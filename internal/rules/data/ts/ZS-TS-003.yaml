id: ZS-TS-003
lifecycle: released
name: document.write() Usage (DOM XSS)
version: "1.0.0"
language: typescript
category: xss
severity: high
confidence: medium
cwe:
  - CWE-79
owasp:
  - "A05:2025"
rationale: |
  document.write() injects its argument directly into the page's HTML
  parser stream, so attacker-controlled input containing <script> tags or
  event handlers executes immediately in the page's origin; TypeScript's
  type system does not track string provenance, so it offers no protection
  once compiled to JavaScript. This is commonly exploitable via third-party
  ad/analytics snippets, URL parameters, or referrer values echoed into a
  document.write() call, and if invoked after the page has loaded it also
  wipes the existing document content.
description: |
  Detects calls to document.write() where the argument traces back to an
  untrusted source (intra-procedural taint tracking).
message: "document.write() detected with a tainted argument — use safe DOM manipulation APIs instead"
references:
  - https://developer.mozilla.org/en-US/docs/Web/API/Document/write
tags: [xss, document-write, dom-injection]
match:
  kind: call
  callee: document.write
  filters:
    - tainted_argument: true
fix_suggestion: "Replace document.write() with safe DOM APIs such as createElement and appendChild."
