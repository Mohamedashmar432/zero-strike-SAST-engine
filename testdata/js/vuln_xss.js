// ZS-JS-002: innerHTML assignment
function renderComment(comment) {
    document.getElementById("output").innerHTML = comment;
}

// ZS-JS-003: document.write
function showData(data) {
    document.write(data);
}
